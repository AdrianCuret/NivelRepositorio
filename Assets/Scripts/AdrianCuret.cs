using UnityEngine;

public class AdrianCuret : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("Adhoper was here");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
